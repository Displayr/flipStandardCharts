#context("Label format")
#Create after DS-1485 completed

#xx <- 1:10
#names(xx) <- c(2001:2005, 2016:2020)
#Check that where axis type is ambiguous, user can alter treatment
#Column(xx, x.tick.format = ".2f")
#Column(xx, x.tick.format = "%b %y")
#Column(xx, x.tick.format = "category")
# What should be the default format
